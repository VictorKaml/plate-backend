🚗 plate-backend

A simple Node.js backend built using Express, CORS, and dotenv.
This backend runs using server.js (as defined in your package.json) and uses CommonJS modules.

📦 Requirements

Make sure you have installed:

Node.js (v14+ recommended)

npm (comes with Node)

📥 Installation

Clone the project and install dependencies:

git clone <https://github.com/VictorKaml/plate-backend>

cd plate-backend
npm install

⚙️ Environment Variables

Create a .env file in the project root:

PORT=3000


Add any other environment variables your server.js uses.

▶️ Running the Server

Start the backend:

npm start


This runs:

node server.js
